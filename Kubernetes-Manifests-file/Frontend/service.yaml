apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: three-tier
spec:
  selector:
    role: frontend
  ports:
    - protocol: TCP
      port: 3000       # البورت اللي هتستخدمه داخل cluster
      targetPort: 80   # البورت اللي التطبيق سامع عليه داخل Pod
  type: ClusterIP
